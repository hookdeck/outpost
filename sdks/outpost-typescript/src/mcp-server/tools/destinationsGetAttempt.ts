/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { destinationsGetAttempt } from "../../funcs/destinationsGetAttempt.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetTenantDestinationAttemptRequest$inboundSchema,
};

export const tool$destinationsGetAttempt: ToolDefinition<typeof args> = {
  name: "destinations-get-attempt",
  description: `Get Destination Attempt

Retrieves details for a specific attempt scoped to a destination.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await destinationsGetAttempt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
