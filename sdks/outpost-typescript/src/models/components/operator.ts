/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Comparison operators for filtering by date-time values (RFC3339 or YYYY-MM-DD format).
 */
export type Operator = {
  /**
   * Filter with value >= the specified date-time.
   */
  gte?: Date | undefined;
  /**
   * Filter with value <= the specified date-time.
   */
  lte?: Date | undefined;
  /**
   * Filter with value > the specified date-time.
   */
  gt?: Date | undefined;
  /**
   * Filter with value < the specified date-time.
   */
  lt?: Date | undefined;
};

/** @internal */
export const Operator$inboundSchema: z.ZodType<
  Operator,
  z.ZodTypeDef,
  unknown
> = z.object({
  gte: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  lte: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  gt: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  lt: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
});
/** @internal */
export type Operator$Outbound = {
  gte?: string | undefined;
  lte?: string | undefined;
  gt?: string | undefined;
  lt?: string | undefined;
};

/** @internal */
export const Operator$outboundSchema: z.ZodType<
  Operator$Outbound,
  z.ZodTypeDef,
  Operator
> = z.object({
  gte: z.date().transform(v => v.toISOString()).optional(),
  lte: z.date().transform(v => v.toISOString()).optional(),
  gt: z.date().transform(v => v.toISOString()).optional(),
  lt: z.date().transform(v => v.toISOString()).optional(),
});

export function operatorToJSON(operator: Operator): string {
  return JSON.stringify(Operator$outboundSchema.parse(operator));
}
export function operatorFromJSON(
  jsonString: string,
): SafeParseResult<Operator, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Operator$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Operator' from JSON`,
  );
}
