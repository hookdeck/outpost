# GetEventMetricsMeasuresUnion

Measures to compute. At least one required. `rate` is events/second throughput. Use bracket notation for multiple values (e.g., `measures[0]=count`).


## Supported Types

### `models.GetEventMetricsMeasuresEnum1`

```python
value: models.GetEventMetricsMeasuresEnum1 = /* values here */
```

### `List[models.GetEventMetricsMeasuresEnum2]`

```python
value: List[models.GetEventMetricsMeasuresEnum2] = /* values here */
```

